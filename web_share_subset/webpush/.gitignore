node_modules/
dist/
.DS_Store

# Cloudflare Workers/Pages static asset limit (~25 MiB per file). App does not fetch this.
public/data/attacking_january_associative_deals_base.json

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
