{
  "name": "website-root",
  "private": true,
  "scripts": {
    "prepare:webpush": "npm --prefix web_share_subset/webpush ci --no-audit --no-fund || npm --prefix web_share_subset/webpush install --no-audit --no-fund",
    "dev": "npm run prepare:webpush && npm --prefix web_share_subset/webpush run dev",
    "build": "npm run prepare:webpush && npm --prefix web_share_subset/webpush run build",
    "preview": "npm run prepare:webpush && npm --prefix web_share_subset/webpush run preview",
    "deploy": "npm run prepare:webpush && npm --prefix web_share_subset/webpush run deploy",
    "worker:dev": "npm run prepare:webpush && npm --prefix web_share_subset/webpush run worker:dev",
    "worker:deploy": "npm run prepare:webpush && npm --prefix web_share_subset/webpush run worker:deploy"
  }
}
